import java.io.Serializable;

import javafx.scene.control.Button;

public class saveScheduleButton extends Button implements Serializable {

    public saveScheduleButton(String buttonName) {
        super(buttonName);
    }
}
